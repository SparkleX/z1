{
	"name": "z1-domain",
	"version": "1.0.0",
	"description": "",
	"main": "dist/index.js",
	"scripts": {
		"prebuild": "domain-builder --in=domain --out=src/db",
		"build": "tsc",
		"clean": "rimraf dist"
	},
	"author": "Sun, Xufei",
	"license": "ISC",
	"dependencies": {},
	"devDependencies": {
		"sparkle-core": "1.0.0",
		"rimraf": "^3.0.0",
		"typescript": "^3.6.4"
	}
}
