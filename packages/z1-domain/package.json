{
	"name": "z1-domain",
	"version": "1.0.0",
	"description": "",
	"main": "dist/index.js",
	"scripts": {
		"prebuild": "json-schema-validator && json2tsdir --input=domain --output=src",
		"build": "tsc",
		"clean": "rimraf dist"
	},
	"author": "Sun, Xufei",
	"license": "ISC",
	"dependencies": {},
	"devDependencies": {
		"core-domain-builder": "1.0.0",
		"json-schema-to-typescript-dir": "^1.0.2",
		"rimraf": "^3.0.0",
		"typescript": "^3.6.4"
	}
}
